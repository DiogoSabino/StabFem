function result = SF_Impedance(baseflow,varargin)
global ff ffdir ffdatadir sfdir verbosity
%%% management of optionnal parameters
    p = inputParser;
    addParameter(p,'k',0.1);
    addParameter(p,'movie','no');
    addParameter(p,'plotaxis','yes');
    parse(p,varargin{:});
    
    solvercommand = ['echo ' num2str(p.Results.k) ' | ' ff  ' Impedance_plot.edp'];
        status = mysystem(solvercommand);
              
        
    result=importFFdata(baseflow.mesh,'AcousticField.ff2m');
    
   if(p
    